class SeatConfig {
  final int seatIndex;
  final String uid;
  final String displayName;
  final bool isBot;

  const SeatConfig({
    required this.seatIndex,
    required this.uid,
    required this.displayName,
    required this.isBot,
  });
}
